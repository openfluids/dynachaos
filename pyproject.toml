[project]
name = "dynachaos"
version = "0.4.1"
description = "Dynamical-systems analysis for time signals with Rust-accelerated kernels"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Ricardo Frantz" }]
keywords = [
    "chaos",
    "dynamical-systems",
    "lyapunov",
    "recurrence",
    "permutation-entropy",
    "coupled-map-lattice",
    "bifurcation",
    "time-series",
    "nonlinear-dynamics",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    # Only versions with published wheels and CI coverage are listed. 3.15 is
    # deliberately absent: it has not been released, so its ABI is not frozen.
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Rust",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = ["numpy>=2.5.1", "scipy>=1.18"]

[project.optional-dependencies]
viz = ["matplotlib>=3.11", "pillow>=12.3"]
dev = ["pytest>=9.1", "maturin>=1.14,<2.0", "ruff>=0.16"]

[project.urls]
Homepage = "https://github.com/openfluids/dynachaos"
Repository = "https://github.com/openfluids/dynachaos"
"Bug Tracker" = "https://github.com/openfluids/dynachaos/issues"

[project.scripts]
dynachaos = "dynachaos.cli:main"

[build-system]
requires = ["maturin>=1.14,<2.0"]
build-backend = "maturin"

[tool.maturin]
exclude = [
    "figures/**",
    "tests/**",
    "benchmarks/results/**",
    "docs/**",
    "examples/**",
]
features = ["pyo3/extension-module"]
module-name = "dynachaos._rust"
python-source = "src"

[tool.uv]
package = true
cache-keys = [{ file = "pyproject.toml" }, { file = "rust/Cargo.toml" }, { file = "rust/src/**/*.rs" }]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]

[dependency-groups]
dev = [
    "pytest>=9.1",
    "pytest-xdist>=3.8",
    "maturin>=1.14,<2.0",
    "ruff>=0.16",
]
