# LaTeX support/build artifacts for paper/
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.toc
paper/compile_*.log
paper/pdflatex_probe.log
paper/rerun_check.log
paper/main.pdf
paper/joss/paper.pdf
paper/.DS_Store

# Private session / planning files — never commit
plan*.md
LEDGER.md
AUDIT.md

# Local task tracker — temporary, must not ship to the public repo
.beads/

# Reference papers (copyrighted): keep parsed .md locally, never ship the sources/figures
pdfs/*
!pdfs/*.md

# README asset generation scripts
assets/gen_*.py

# OS / filesystem noise
.DS_Store

# Python bytecode / cache
__pycache__/
*.pyc
*.pyo

# Python tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local environments
.venv/
venv/

# Rust build artifacts
target/

# Python build artifacts
dist/
build/
*.egg-info/

# Compiled extensions
*.so
*.dylib
*.pyd

# Computed data — large, reproducible from scripts
examples/*.npz
figures/**/*.npz
# heavy animation frames + rendered gifs are not shipped (regenerable from scripts)
figures/**/*animation*.npz
figures/**/*.gif

# Rendered gallery site — regenerated by scripts/build_gallery.py and by CI
site/

# Matplotlib font/render cache (created during figure runs)
.mpl-cache/

# supervisor-coder AUTO-mode dispatch scratch
.sc-dispatch.jsonc
.sc-out.txt
